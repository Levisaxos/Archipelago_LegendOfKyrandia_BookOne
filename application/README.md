# application/

The future installable application: the **forked ScummVM engine** (Book 1 =
`KyraEngine_LoK`) with the embedded Archipelago client (apclientpp) wiring.

End state: a build the user points at their own GOG Kyrandia data files, which
runs the game with AP item/location shuffle in the middle.

_Empty for now — to be filled once the C++ build toolchain is set up and the
engine fork begins._
